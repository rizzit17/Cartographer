/** Cartographer — Diff Viewer Page stub. */
export default function DiffViewer() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Diff Viewer</h1>
      <p className="text-muted-foreground mt-1">PR-ready git diffs from sandbox execution</p>
      <div className="mt-6 glass-card rounded-xl p-8 flex items-center justify-center">
        <p className="text-muted-foreground text-sm">Phase 3/6 — Full implementation coming</p>
      </div>
    </div>
  )
}
