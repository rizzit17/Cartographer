/** Cartographer — Repository Explorer Page stub. Monaco editor implementation Phase 3. */
export default function RepositoryExplorer() {
  return (
    <div className="p-6 h-full flex flex-col">
      <h1 className="text-2xl font-bold">Repository Explorer</h1>
      <p className="text-muted-foreground mt-1">Browse and explore repository files</p>
      <div className="mt-6 glass-card rounded-xl flex-1 flex items-center justify-center">
        <p className="text-muted-foreground text-sm">Monaco Editor — Phase 3</p>
      </div>
    </div>
  )
}
