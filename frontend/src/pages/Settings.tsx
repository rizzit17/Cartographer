/** Cartographer — Settings Page stub. */
export default function Settings() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Settings</h1>
      <p className="text-muted-foreground mt-1">Configure LLM providers, embeddings, and preferences</p>
      <div className="mt-6 glass-card rounded-xl p-8 flex items-center justify-center">
        <p className="text-muted-foreground text-sm">Phase 3 — Full implementation coming</p>
      </div>
    </div>
  )
}
